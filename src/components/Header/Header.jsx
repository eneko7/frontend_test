import React from 'react';
import styles from './Header.scss';

const Header = () => (
  <header className={styles.header}>
    <span className={styles.logo}>UGRepositories</span>
  </header>
);

export default Header;
